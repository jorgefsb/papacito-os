# IONOS Deploy Now Configuration
# Papacito OS - Second Brain

build:
  command: cd my-app && npm ci && npm run build
  output: my-app/dist

runtime:
  type: nodejs20
  start: cd my-app && npm start
  
env:
  - name: DATABASE_URL
    value: postgresql://neondb_owner:npg_CU3jrqws7bOD@ep-late-grass-air7fbpj-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require
  - name: NODE_ENV
    value: production
